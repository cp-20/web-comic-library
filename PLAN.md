# Web漫画読書管理サービス開発計画

最終更新日は2026年7月24日とする。

## プロダクトの目的

Web漫画の更新確認、読書進捗、単行本の既読と所蔵、読者同士の交流を一つにまとめた、日本向けの公開Webサービスを作る。

[Annict](https://annict.com/)の作品管理とソーシャル機能を漫画向けに置き換え、[Web漫画アンテナ](https://webcomics.jp/site)を参考に複数サイトの更新収集機能を加える。

Web話を中心に設計するが、単行本だけを読む利用者も同じ作品データを使って管理できるようにする。

初期クライアントはスマートフォンを中心としたレスポンシブWebとPWAとし、ネイティブアプリは利用実績を確認してから判断する。

## 公開ベータの成功条件

- 登録から作品検索、フォローまでをスマートフォンから3分以内に完了できる。
- 対応サイトの更新の95％以上を、公開から30分以内に検出する。
- 更新検出後、Web Pushの95％以上を5分以内に送信する。
- 出版社サイトとニコニコ漫画に同じ内容が掲載されても、作品と読書進捗を不必要に重複させない。
- Web話を操作しない利用者も、単行本の既読、所蔵、新刊通知を利用できる。
- 公開した読書記録をSNSへ共有し、共有先から作品ページまたは活動ページへ遷移できる。

## 機能要件

### アカウント

- メールリンクまたはGoogle OAuthで登録とログインを行える。
- 表示名、ユーザーID、自己紹介、アイコンを設定できる。
- アカウント全体の標準公開範囲を、公開、フォロワー限定、非公開から選べる。
- 初回設定で公開範囲を選ぶまでは非公開として扱う。
- 作品、既読記録、所蔵記録、感想ごとに公開範囲を上書きできる。
- 利用者データをJSONでエクスポートできる。
- アカウントを削除でき、削除後は即時に第三者から閲覧できなくなる。

### 作品カタログ

- 作品名、別名、作者名から作品を検索できる。
- 掲載先、連載状態、公式作品かユーザー投稿かで絞り込める。
- 作品ページで作者、概要、連載状態、掲載先、Web話、単行本、最近の公開活動を確認できる。
- 同じ作品に複数の掲載先を関連付けられる。
- 公開された閲覧ページへのリンクを提供し、漫画本文をサービス内で配信しない。
- 最近更新された作品、人気作品、新着作品を閲覧できる。

### 読書状態

利用者が作品に設定する状態は次の5種類とする。

- **読みたい**：今後読む予定がある。
- **読んでいる**：現在読み進めている。
- **一時中断**：再開する可能性がある。
- **読むのをやめた**：継続して読む予定がない。
- **読み切った**：利用者が読了した。

最新話まで読んでいることは、手動状態とは別の**追いつき状態**として自動計算する。

作品自体の連載状態は、連載中、休載、完結、不明として、利用者の読書状態と分離する。

### Web話の進捗

- 一話または一部分ごとに既読を記録できる。
- 指定した話までを一括で既読にできる。
- 既読を取り消せる。
- 実際に読んだ掲載先を記録できる。
- 同一内容だと確認された別掲載先にも既読状態を反映できる。
- 分割掲載と一括掲載の対応が未確認の場合は、誤った既読状態を作らず掲載先ごとの記録を保持する。

### 漫画サイトのお気に入りインポート

- WXTでChromeとFirefox向けのブラウザ拡張を提供する。
- 利用者が許可した漫画サイトのお気に入りページから、作品ID、作品URL、作品名だけを端末内で抽出する。
- ブラウザ履歴、ブックマーク、Cookieを読み取る権限は要求しない。
- 抽出した作品をカタログへ照合し、利用者の確認後に読書状態とフォロー方式を一括登録する。
- お気に入り登録だけから既読、読書状態、読書進捗を推測しない。
- お気に入りから消えた作品の読書記録を自動削除しない。
- 非公開API、認証情報、漫画本文、漫画ページ画像を取得しない。

### フォロー方式

利用者は作品ごとに次の通知方式を選べる。

- **最速**：いずれかの掲載先で公開された時点で一度だけ通知する。
- **サイト優先**：利用者が設定したサイト優先順位に従い、最上位の掲載先で公開された時点で通知する。
- **掲載先指定**：その作品について選択した掲載先だけを通知する。
- **全掲載先**：各掲載先への公開ごとに通知する。

掲載先の優先順位は作品ごとに持たせず、利用者単位で一つの順序付きリストとして保持する。

たとえば、利用者が「ニコニコ漫画、少年ジャンプ＋、カドコミ」の順に設定した場合、すべての作品で同じ順位を使う。

「サイト優先」を選んだ作品が優先リスト内のどのサイトにも掲載されていない場合は、最初に検出した公式掲載先へフォールバックする。

優先している掲載先が終了または無効になった場合は、次に順位が高い有効な掲載先を使う。

「掲載先指定」と「全掲載先」で同じ内容が複数回通知されるのは利用者が選んだ挙動として扱うが、アプリ内では同じ話の掲載情報を一つのまとまりとして表示する。

### 単行本の進捗と所蔵

- 単行本はISBNまたは出版社の商品IDごとの版として管理する。
- 各巻を未読、読書中、既読として記録できる。
- 紙で所蔵、電子で所蔵、紙と電子の両方で所蔵、のいずれかを記録できる。
- 巻に収録される話と範囲を記録できる。
- 巻を既読にしたとき、確認済みの対応情報があればWeb話の進捗へ反映する。
- 巻とWeb話の対応が未確認でも、巻の既読と所蔵を保存できる。
- 利用者は個人用の記録として、その巻で第何話まで読んだかを入力できる。
- 利用者が入力した対応情報は運営への修正候補とし、確認前に他の利用者へ適用しない。
- フォロー中の作品について新刊発売を通知できる。

購入価格、購入店、貸出管理は初期版に含めない。

### 更新通知

- アプリ内通知を常時利用できる。
- Web Pushはブラウザの許可を得た利用者へ即時送信する。
- メールは利用者が有効にした場合だけ、更新があった日のダイジェストとして送信する。
- 新しい話、新刊、フォロー、いいねを通知種別ごとに設定できる。
- 新しい通常話、番外編、再掲載、公開期間変更、告知を区別する。
- 通常話と番外編は初期状態で通知し、再掲載、公開期間変更、告知は利用者が有効にした場合だけ通知する。
- 同じイベントを再処理しても同じ経路へ二重送信しない。
- 過去データの初回取り込みでは通知を発生させない。
- 期限切れまたは無効になったPush購読を自動的に停止する。

### ソーシャル

- 公開プロフィールを提供する。
- 利用者をフォロー、フォロー解除できる。
- フォロワー限定または非公開アカウントへのフォローは承認制にできる。
- フォロー中の利用者の活動をタイムラインで確認できる。
- 読了、読書状態の変更、感想を活動として公開できる。
- 活動を作らずに読書記録だけを更新できる。
- 話または巻に紐付けて1,000文字以内の感想を投稿できる。
- 活動または感想へいいねできる。
- 利用者をブロックまたはミュートできる。
- 活動、感想、プロフィールを通報できる。
- 運営は通報対象の非表示、警告、利用停止を行える。

ダイレクトメッセージ、スレッド形式の返信、感想への画像添付は初期版に含めない。

### ネタバレ制御

- 感想を話または巻へ紐付ける。
- 投稿者が感想をネタバレとして明示できる。
- 閲覧者が未読の話より先の感想を伏せて表示する。
- 未ログイン状態では話に紐付いた感想を初期状態で伏せる。
- 閲覧者が操作した場合だけ伏せた感想を表示する。
- ネタバレ本文を通知、OG画像、SNS共有文へ含めない。

### SNS共有

- スマートフォンではWeb Share APIを利用できる。
- X、Bluesky、LINE向けの共有リンクを提供する。
- 作品、読了記録、感想、プロフィールに安定した公開URLを持たせる。
- 作品名、読書状態、利用者名を使った動的OG画像を生成する。
- 非公開情報とネタバレ本文を共有ページへ含めない。
- 公開ページにはcanonical URL、OGメタデータ、構造化データを設定する。

### ニコニコ漫画の収録

最終的には、公開一覧から取得できるシリーズ形式のユーザー投稿漫画を可能な限り収録する。

実装と取り込みは次の優先順位で進める。

1. 公式作品と他サイトからの出張掲載を収録する。
2. 直近90日以内に更新されたユーザー投稿作品を収録する。
3. 新着一覧やランキングに掲載されたユーザー投稿作品を収録する。
4. 公開一覧を低速で走査し、残りのユーザー投稿作品をバックフィルする。
5. 長期休載作品と完結作品を低頻度で再確認する。

過去作品の全件取り込みは公開ベータの必須条件にせず、公開後も継続する収録マイルストーンとする。

削除済み、限定公開、年齢制限付き、漫画シリーズとして識別できない単発画像は対象外とする。

公式作品とユーザー投稿作品をデータ上で区別し、検索と絞り込みに反映する。

### 成人向け作品

掲載先がR18、年齢確認必須、成人向けとして分類した作品は収集しない。

成人向け作品は検索、ランキング、通知、共有の対象にも含めない。

年齢制限ページへのアクセスや、分類を目的とした漫画本文の解析は行わない。

掲載先のメタデータ、書誌情報、利用者からの通報に基づいて除外する。

### 運営機能

- 作品、作者、掲載先、話、巻を修正できる。
- 重複作品と重複話の統合、誤統合の分割を行える。
- 統合と分割の操作履歴を監査できる。
- コネクタごとの最終成功時刻、取得件数、失敗理由、再試行状態を確認できる。
- 解析に失敗したデータを確認し、再実行できる。
- 利用者からの作品追加申請と修正候補を処理できる。
- 通報、ブロック状況、利用停止を管理できる。
- 掲載先単位で収集を緊急停止できる。

## 収集対象

初期候補は次の順とする。

1. ニコニコ漫画
2. 少年ジャンプ＋
3. カドコミ
4. コミックDAYS
5. となりのヤングジャンプ
6. ヤンマガWeb
7. サンデーうぇぶり
8. マガポケ
9. ガンガンONLINE
10. アルファポリス

各サイトの利用規約、robots.txt、公式API、フィード、許諾手段を確認する。

自動取得できない候補は、webアクション、MAGCOMI、くらげバンチ、チャンピオンクロス、ヤンチャンWebの順で置き換え、公開ベータでは許可された10サイトへの対応を目標とする。

ニコニコ漫画について許可された更新確認手段を確保できない場合は、認証やアクセス制限を回避せず、収録方法または公開条件を見直す。

### 2026年7月24日のHTTP取得検証

公開ページのメタデータだけを対象に、ログインとブラウザ自動操作を使わずHTTP GETで取得できるかを検証した。

漫画本文とページ画像は取得していない。

| 対象                                                                     | 検証した入口                                 | HTTPだけで取得できた情報                                                                                              | 実装方針                 |
| ------------------------------------------------------------------------ | -------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| [少年ジャンプ＋](https://shonenjumpplus.com/atom)                        | Atomと各話ページ                             | 検証時116件の`entry`、更新日時、各話URL、作者、作品名、話名                                                           | 共通フィード型コネクタ   |
| [コミックDAYS](https://comic-days.com/atom)                              | Atomと各話ページ                             | 検証時402件の`entry`、更新日時、各話URL、作者、作品名、話名                                                           | 共通フィード型コネクタ   |
| [となりのヤングジャンプ](https://tonarinoyj.jp/atom)                     | Atomと各話ページ                             | 検証時41件の`entry`、更新日時、各話URL、作者、作品名、話名                                                            | 共通フィード型コネクタ   |
| [ニコニコ漫画](https://manga.nicovideo.jp/manga/list?sort=manga_updated) | 更新順一覧、ページ送り、作品ページ           | 一覧は20件単位で、作品名、作者、開始日、更新日、公開話数を取得できた。作品ページから話名、話URL、公開状態を取得できた | ニコニコ漫画専用コネクタ |
| [カドコミ](https://comic-walker.com/)                                    | トップ、作品ページ、HTML内の`#__NEXT_DATA__` | 作品コード、作品名、作者と役割、連載状態、次回更新日、話コード、話名、更新日時、公開状態を取得できた                  | カドコミ専用コネクタ     |

少年ジャンプ＋、コミックDAYS、となりのヤングジャンプは、Atomと各話ページの構造が共通している。

この3サイトは、ベースURLとフィードURLだけを設定として渡す一つの共通フィード型コネクタで扱い、サイトごとのフィクスチャで差異を検出する。

各話ページには作品別の`/rss/series/{id}`へのリンクがあり、3サイトすべてでHTTPだけで取得できた。

全体Atomを新着作品と更新の発見に使い、作品別RSSを初回の公開話履歴と作品単位の差分確認に使う。

ニコニコ漫画の更新順一覧は`/manga/list?sort=manga_updated`で、`page`クエリによるページ送りができる。

通常巡回は更新順の先頭から前回の透かし位置までを確認し、更新された作品ページだけを取得する。

初回バックフィルは作成順一覧も低速で走査し、ページ番号と最終作品IDをチェックポイントとして保存する。

ニコニコ漫画の公式作品は、検証時には作品ページの公式チャンネルへのパンくずリンクでユーザー投稿作品と区別できた。

この判定規則が欠けた場合はユーザー投稿として推測せず、種別不明として管理者確認へ送る。

カドコミは、サーバーから返るHTML内の`#__NEXT_DATA__`に作品と話の構造化データが含まれていた。

公開HTMLに埋め込まれたJSONだけを`JSON.parse`し、必要な部分をValibotで検証する。

ページ内から判明した非公開仕様のAPIを直接呼び出す実装にはしない。

埋め込みJSONが消えた場合やスキーマが変わった場合は、安定したHTML要素から必要最小限を補完し、それも失敗したら既存データを維持してコネクタを停止する。

カドコミの検証例では、年齢区分らしき`ratingLevel`が公開ページでも`adult`だった。

この値だけでR18と断定せず、掲載元が公開する定義と年齢確認の有無を確認した対応表を`SourcePolicyRecord`に持たせる。

対応表が未確認の値は公開カタログへ自動投入せず、R18または年齢確認必須と確認できた作品は取得対象から除外する。

検証時点では5対象すべてをHTTPだけで処理できたため、本番の巡回コンテナへPlaywrightやChromiumを含めない。

将来の候補がJavaScript実行なしでは取得できない場合も、自動的にブラウザ取得へ切り替えず、未対応として許諾手段や別の公式フィードを検討する。

robots.txtはニコニコ漫画で空の200応答、少年ジャンプ＋、コミックDAYS、となりのヤングジャンプで404応答だった。

カドコミのrobots.txtは検証時にタイムアウトし、内容を確認できなかった。

robots.txtが空または存在しないことは取得許諾を意味しない。

以上は技術検証であり、本番収集は各サイトの利用規約、フィードの用途、問い合わせ結果を`SourcePolicyRecord`へ記録してから有効化する。

## システム設計

### 全体構成

```text
利用者
  |
  v
既存のCloudflare Tunnel
  |
  +-- /      --> Next.js Web/PWA Service
  |
  +-- /api/* --> Hono API Service
                    |
                    v
             Asterion k3sクラスタ
               +-- Next.js Web Deployment
               +-- Hono API Deployment
               +-- 巡回と通知ワーカーDeployment
               +-- 既存PostgreSQL StatefulSet
                          |
                          +--> Cloudflare R2へバックアップ

漫画サイト、書誌API
  |
  +--> 巡回と通知ワーカー

GitHub Actions --> GHCR --> Argo CD --> Asterion k3sクラスタ
```

Web frontendはNext.js、backend APIはHonoとして別workspaceと別Deploymentにする。

巡回、書誌同期、通知、メール送信は同じリポジトリの常駐ワーカーで処理する。

APIとワーカーは同じdomain層とapplication層を共有する。

Webからbackendを呼び出す場合は、`hc<ApiType>`を包んだHono RPC clientだけを使う。

APIとワーカーからデータアクセス層を直接呼ばず、applicationが宣言するportをinfrastructure adapterが実装する。

公開ベータまでは、既存のAsterion k3sクラスタへWeb、API、ワーカーを別Deploymentとして配置する。

ワーカーにはrequestsとlimitsを設定し、巡回の過負荷がWebと既存ワークロードを巻き込まないようにする。

Redis、専用検索エンジン、マイクロサービスは、PostgreSQLと単一ワーカーで測定上の限界が確認されるまで導入しない。

### アーキテクチャ

層を分けたモジュール化モノリスを採用する。

デプロイ単位をWeb、API、ワーカーに分け、コード上ではdelivery、application、domain、infrastructure adapter、contractsを分離する。

依存はdeliveryとadapterからapplication、applicationからdomainへ向ける。

domainはフレームワーク、DB、外部HTTP、環境変数、Valibotへ依存しない。

applicationはuse case、認可、トランザクション境界、repositoryなどのportを持ち、Drizzle、Cheerio、Better Auth、Graphile Worker、通知SDKへ依存しない。

Drizzle、コネクタ、認証、通知はapplicationのportを実装するadapterとする。

Webは`@web-comic-library/api-client`だけを通してHono APIへアクセスし、backendの関数やapplicationを直接importしない。

Hono handlerとワーカージョブはapplication use caseだけを呼び、Drizzleや外部SDKを直接呼ばない。

更新処理はdomainの状態遷移を通し、参照処理はapplicationのquery portから用途別のread modelを返せるようにする。

状態変更と通知用eventは同じPostgreSQLトランザクションへ保存し、外部通知はcommit後にワーカーが冪等に処理する。

モジュールはcatalog、library、releases、social、identity、moderationに分け、別モジュールのaggregateを直接変更しない。

物理構成、port、transaction、validation、error、テストの規則は[アーキテクチャ規約](docs/architecture.md)を正とする。

リポジトリ全体の実装規則は[AGENTS.md](AGENTS.md)から参照する。

### Asterionへの統合

[asterion-manifest](https://github.com/cp-20/asterion-manifest)へ`web-comic-library`ディレクトリとproduction overlayを追加する。

既存のApplicationSetがトップレベルのディレクトリを検出するため、個別のArgo CD Applicationは作らない。

マニフェストにはWeb Deployment、API Deployment、ワーカーDeployment、各ClusterIP Service、マイグレーションJob、ConfigMap、SOPSで暗号化したSecretを置く。

選定した公開ホスト名を既存のcloudflared設定へ追加し、ClusterIP Serviceへ接続する。

NodePort、LoadBalancer、家庭内ルーターのポート開放は使わない。

コンテナはGitHub Actionsで構築してGHCRへ公開し、asterion-manifestでイメージを固定する。

Argo CDの自動同期と自己修復、既存のRenovate設定をそのまま利用する。

[asterion-ansible](https://github.com/cp-20/asterion-ansible)はホストOSとk3sノードの管理に限定し、アプリのDeploymentを追加しない。

ホスト上のPostgreSQLロールは使わず、k3s内のPostgreSQL Serviceへ接続する。

既存のPostgreSQL 16へ専用データベースと最小権限の専用ロールを作り、初期版では別のPostgreSQL StatefulSetを増やさない。

データベースとロールは、PostgreSQL側のマニフェストへ置く冪等なJobで作成する。

DBマイグレーションは、APIとワーカーのデプロイ前に一度だけ動くArgo CDのPreSync Jobとして実行する。

アプリのアイコン、生成したOG画像、バックアップはlocal-pathへ置かず、Cloudflare R2へ保存する。

初期のリソース設定は次の値から始め、負荷試験後に修正する。

| Pod      | CPU requests | CPU limits | メモリrequests | メモリlimits |
| -------- | -----------: | ---------: | -------------: | -----------: |
| Web      |         250m |          1 |          256Mi |        768Mi |
| API      |         125m |          1 |          128Mi |        512Mi |
| ワーカー |         250m |          1 |          256Mi |        768Mi |

ブラウザを起動しない巡回に限定することで、ワーカーのメモリ上限とコンテナイメージを小さく保つ。

公開ベータへ進むには、既存ワークロードのピーク時にも2GiB以上のメモリと1コア相当のCPU余力が必要である。

### 中心となるデータ

- **User**：認証情報とアカウント状態。
- **Profile**：表示名、ユーザーID、自己紹介、アイコン、標準公開範囲。
- **Work**：媒体をまたいだ漫画作品。
- **WorkAlias**：表記違い、旧題、読み仮名。
- **Creator**：作者、原作者、作画担当などの人物または団体。
- **Source**：漫画サイトまたは書誌データ提供元。
- **Publication**：サイトごとの連載ページ。
- **ContentUnit**：作品内の論理的な話または話の一部分。
- **PublicationEntry**：掲載先に存在する実際の閲覧ページと、通常話、番外編、再掲載、告知、不明の種別。
- **EntryContentMapping**：掲載ページと論理的な話の多対多対応。
- **VolumeEdition**：ISBNまたは商品IDを持つ単行本の版。
- **VolumeContentMapping**：巻と収録話の多対多対応と確認状態。
- **LibraryEntry**：利用者の読書状態、通知方式、公開範囲。
- **UserSourcePreference**：利用者単位の掲載先優先順位。
- **SubscriptionPublication**：掲載先指定時に選ばれた連載ページ。
- **ContentReadRecord**：利用者の話単位の既読記録。
- **PublicationReadRecord**：実際に読んだ掲載ページの記録。
- **UserVolumeRecord**：巻の読書状態、紙と電子の所蔵状態、個人用の話数メモ。
- **UserFollow**：利用者間のフォロー状態。
- **Activity**：読了、状態変更、感想などの公開可能な活動。
- **Reaction**：活動または感想へのいいね。
- **Block**：ブロックとミュート。
- **Report**：利用者からの通報。
- **ReleaseEvent**：新しい話、再掲載、公開期間変更、新刊を検出した事実。
- **Notification**：送信対象、経路、既読状態、冪等性キー。
- **PushSubscription**：ブラウザのPush購読情報。
- **CrawlRun**：取得日時、応答状態、解析結果、失敗理由。
- **SourceCrawlState**：掲載元単位の透かし位置、ページ番号、最終更新日時、連続失敗数。
- **FetchResourceState**：取得URL単位のETag、Last-Modified、本文ハッシュ、最終成功日時。
- **MergeAudit**：作品、話、巻の統合と分割の履歴。
- **SourcePolicyRecord**：取得条件、営利利用、広告利用、アフィリエイト利用、年齢区分の対応表の確認結果と確認日。

### 重複判定

掲載先の外部IDと正規化URLが一致するデータは同一の掲載データとして扱う。

作品名と作者が正規化後に完全一致し、矛盾する媒体情報がない場合だけ作品を自動統合する。

掲載元が同一作品への正式なリンクを提供している場合は、その関係を優先する。

話数、枝番、題名が一致する1対1掲載だけを自動対応付けする。

全話版と分割版、作者不明、同名作品、コミカライズ違い、版によって構成が異なる単行本は管理者の確認対象にする。

分割掲載と一括掲載は多対多の対応として保持する。

対応が未確認の掲載ページから、別掲載先の既読を自動的に推測しない。

重複掲載が後から見つかった場合も、既読記録、所蔵記録、通知履歴を失わず再関連付けする。

統合後の旧URLと旧IDは新しい正規データへリダイレクトする。

機械学習による自動統合は初期版に含めず、決定的な規則と管理者確認を使う。

### 収集処理

- サイトごとに型付きコネクタを実装する。
- `Bun.fetch`によるHTTP取得、レート制御、文字コード処理、差分保存、入力検証、ログ記録は共通処理にする。
- 更新一覧を15分間隔で確認し、変更が見つかった作品だけ詳細を取得する。
- 活動中のユーザー投稿作品を優先し、長期休載作品は日次または週次で確認する。
- 接続先を許可済みホストへ限定する。
- `redirect: "manual"`で最大3回まで追跡し、各リダイレクト先も許可済みホストか検証する。
- ホストごとの同時接続数を1に制限する。
- 通常巡回はホストごとに最低2秒の間隔と揺らぎを置き、バックフィルはさらに低速にする。
- ETagとLast-Modifiedを保存し、`If-None-Match`と`If-Modified-Since`による条件付きGETを使う。
- 304応答では本文を解析せず、最終確認日時だけを更新する。
- `Retry-After`を優先し、それがなければ指数バックオフし、連続失敗時はコネクタを一時停止する。
- User-Agentにサービス名と連絡先を含める。
- タイムアウトを10秒、応答本文の上限を5MiBから始め、`Content-Type`と実際の読込量を検証する。
- 認証、CAPTCHA、ペイウォール、年齢確認を回避しない。
- 実行時には漫画本文とページ画像を保存しない。
- 正規化したメタデータ、コンテンツハッシュ、閲覧先URLだけを長期保存する。
- HTML構造変更を検出した場合は既存データを削除せず、失敗として管理者へ通知する。

取得手段の優先順位は、利用可能な公式API、公式AtomまたはRSS、公開一覧HTML、公開作品HTML、公開HTML内の埋め込みJSONとする。

HTMLとXMLはCheerio、埋め込みJSONは`JSON.parse`で解析する。

解析結果は外部入力として扱い、Valibotの`safeParse`を通過した候補だけを正規化処理へ渡す。

必須フィールドの欠落、日時形式の異常、想定外の列挙値は、空値で補完せず解析失敗として記録する。

コネクタの責務は、更新候補の発見と作品詳細の取得に分ける。

```ts
interface Connector {
  discover(context: CrawlContext): Promise<DiscoveryBatch>;
  fetchPublication(ref: PublicationRef): Promise<PublicationCandidate>;
}
```

`DiscoveryBatch`は候補、次のチェックポイント、取得元の更新時刻を返す。

チェックポイントは取得成功とデータ保存を同じトランザクションで完了した後にだけ進める。

コネクタは次の構成で分離する。

```text
apps/
  web/
  api/
  worker/
  extension/
packages/
  api-client/
  domain/
  application/
  contracts/
  db/
  connectors/
    core/
    common-feed/
    niconico/
    kadocomi/
  auth/
  notifications/
```

`api-client`はHono RPC、`domain`は業務ルールと不変条件、`application`はuse caseとport、`contracts`はHTTPとjob payloadのValibot schemaを持つ。

`db`、`connectors`、`auth`、`notifications`はapplication portのadapterとする。

`connectors/core`はHTTPクライアント、ホスト別レート制御、条件付きGET、Valibotスキーマ、正規化処理を持つ。

`common-feed`は少年ジャンプ＋、コミックDAYS、となりのヤングジャンプの全体Atomを走査し、各話ページから作品名と作品別RSSを補完する。

`niconico`は更新順一覧を差分巡回し、作品ページから公開話を取得する。

`kadocomi`は公開HTML内の`#__NEXT_DATA__`を第一候補として解析し、公開HTMLを限定的なフォールバックにする。

コネクタごとの最初の抽出規則は次のとおりとする。

| コネクタ       | 発見処理                                                                                                                                   | 詳細処理                                                                                                                         | 外部キー                         |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- |
| 共通フィード型 | 全体Atomの`entry`から`title`、`updated`、`link[href]`、`author > name`を読み、各話ページの`a[href*="/rss/series/"]`から作品別RSSを発見する | 作品別RSSの`item`と、各話ページの`h1.episode-header-title`、`h1.series-header-title`、`h2.series-header-author`を読む            | 正規化した各話URL                |
| ニコニコ漫画   | `li.mg_item.item`から`.mg_title .title a`、`.mg_author`、`.date.updated`、`.serial_status`を読む                                           | `li.episode_item`ごとに`div.description div.title a`と`.status`を読む                                                            | `/comic/{id}`と`/watch/{id}`のID |
| カドコミ       | 公開ページの`script#__NEXT_DATA__`を読む                                                                                                   | `dehydratedState.queries[].state.data`からValibotの形に一致する`work`と`firstEpisodes.result`または`latestEpisodes.result`を読む | `work.code`と`episode.code`      |

ニコニコ漫画では45話を持つ検証作品について、1回の作品ページ取得から45個の`li.episode_item`を取得できた。

カドコミでは127件の公開項目を持つ検証作品について、1回の作品ページ取得から127件を埋め込みJSONとして取得できた。

この127件には通常話だけでなく単行本告知や記念イラストも含まれていた。

すべてを`PublicationEntry`として保持し、通常話と番外編以外は追いつき状態の分母へ自動的に含めない。

種別を決定できない項目は不明として保存し、題名だけから通常話へ推測しない。

CSSクラスや埋め込みJSONの内部配置は外部契約ではないため、抽出件数が急減した場合は「更新なし」とせずスキーマ変更として停止する。

カドコミではハッシュ付きCSSクラスを主要セレクタに使わず、埋め込みJSONの形と作品コード、話コードを優先する。

ニコニコ漫画の全件バックフィルではワーカーを増強せず、通常巡回を優先する低優先度キューを余剰CPUの範囲で処理する。

### 書誌情報

[openBD](https://openbd.jp/)を第一候補として、ISBN、書名、作者、出版社、発売日、許諾された書影を取得する。

openBDで不足する書誌は[国立国会図書館サーチ](https://ndlsearch.ndl.go.jp/help/api)で補完する。

データ提供元、取得日時、利用条件をフィールド単位で追跡できるようにする。

openBDの変更と削除を定期同期し、削除要請へ対応できるようにする。

近刊情報の精度や収録範囲が不足した段階で、有償のJPRO Web APIを検討する。

### 検索

作品名、別名、読み仮名、作者名をUnicode NFKCで正規化する。

PostgreSQLの`pg_trgm`と前方一致を使い、短い日本語タイトルは完全一致と前方一致を優先する。

検索品質または応答時間が目標を満たさなくなった場合だけ、専用検索エンジンを追加する。

### タイムライン

初期版では、フォロー関係と公開可能な活動を問い合わせ時に結合する。

利用者ごとのタイムラインを事前生成しない。

問い合わせ時間が性能目標を超える状態が継続した場合、ページ単位のキャッシュまたはファンアウト方式を検討する。

### 公開インターフェース

作品、プロフィール、公開活動には変更しにくい安定したURLを割り当てる。

作品や利用者を統合しても旧URLから新URLへリダイレクトする。

初期版では第三者向け公開JSON APIを提供しない。

アプリ内部の取得と更新はHono APIとして実装し、Hono RPCでWebへ型を共有する。

Hono handlerの入力は`@hono/valibot-validator`とValibotで検証する。

WebはHono RPC clientを経由し、backendの関数を直接importまたは実行しない。

コネクタは正規化済みの作品候補、掲載ページ候補、話候補を返す内部インターフェースへ統一する。

## 技術スタック

### 採用技術

- **ランタイム**：Bun 1.3.14。開発環境、CI、コンテナで同じ版へ固定する。
- **パッケージ管理**：Bun workspacesと`bun.lock`。CIでは`bun install --frozen-lockfile`を使う。
- **formatter**：[oxfmt](https://oxc.rs/docs/guide/usage/formatter)。
- **lint**：[oxlint](https://oxc.rs/docs/guide/usage/linter)。循環依存と主要な逆向き依存も検査する。
- **Web**：Next.js 16 App Router、React 19、TypeScript 6.0.3 strict。
- **backend API**：HonoとHono RPC。
- **ブラウザ拡張**：[WXT](https://wxt.dev/) 0.20.27。ChromeとFirefoxを対象にする。
- **UI**：Tailwind CSS。
- **入力検証**：Valibot。
- **データベース**：Asterionで稼働中のPostgreSQL 16。
- **データアクセス**：Drizzle ORMとSQLマイグレーション。
- **非同期処理**：[Graphile Worker](https://worker.graphile.org/docs/cron)。
- **認証**：[Better Auth](https://better-auth.com/docs/integrations/hono)。
- **HTTP取得**：`Bun.fetch`。
- **HTMLとAtom解析**：Cheerio。
- **Web Push**：標準Service Workerと`web-push`。
- **メール**：[Resend](https://resend.com/pricing)。
- **オブジェクト保存**：[Cloudflare R2](https://developers.cloudflare.com/r2/pricing/)。
- **データベースバックアップ**：WAL-G。
- **エラー監視**：Sentry。
- **単体と統合テスト**：`bun test`。
- **E2Eテスト**：Playwright。ただしCIと開発環境だけで使い、本番イメージには含めない。
- **CI**：GitHub Actions。
- **コンテナレジストリ**：GitHub Container Registry。
- **実行基盤**：Asterion k3sクラスタ。
- **外部公開**：既存のCloudflare Tunnel。
- **継続的デプロイ**：既存のArgo CD ApplicationSet。
- **インフラ定義**：Dockerfile、Kubernetesマニフェスト、Kustomize。
- **Secret管理**：既存のSOPS、age、KSOPS。

Web、API、ワーカー、マイグレーションはmonorepoを共有し、用途別のDocker build targetから別イメージを作る。

本番イメージはBunのslim系イメージを使うマルチステージビルドとし、開発依存、Playwright、ブラウザバイナリを含めない。

Graphile Workerを使ってPostgreSQLをジョブキューにも利用し、初期版ではRedisを持たない。

Prettier、ESLint、Biomeは併設しない。

CIは`bun run check`でoxfmtの差分とoxlintの警告を失敗として扱う。

第0段階でNext.js、Hono、Hono RPC、Better Auth、Drizzle ORM、Graphile Worker、`web-push`をBun上で一つずつ起動し、Node.js固有APIへの非互換がないことを確認する。

非互換があった依存だけを同じ責務のBun対応ライブラリへ置き換え、Node.jsランタイムを併設する構成にはしない。

PWAのService WorkerはPush受信と通知クリックに必要な最小機能だけを実装し、大規模なオフライン同期は行わない。

PostgreSQL 18への更新はこのサービスだけでは行わず、Asterion全体のバックアップと復元試験を終えた後に別作業として判断する。

### 比較した選択肢

- Ruby on RailsはCRUD、ジョブ、メールを一つにまとめやすいが、TypeScriptとReactを使う開発方針に合わないため採用しない。
- NestJSはHonoより実行時依存と構成要素が増えるため採用しない。
- Vercelだけで構成すると常駐巡回ワーカーを別サービスへ出す必要があるため採用しない。
- Firebase中心の構成は認証とリアルタイム更新に強いが、作品統合と多対多関係をPostgreSQLより扱いにくいため採用しない。
- VPSやRenderは家庭回線から独立できるが、Asterionに余力がある間は固定費が重複するため採用しない。
- 新しいPostgreSQL StatefulSetは障害範囲を分けられるが、メモリ、保存領域、バックアップ対象が増えるため初期版では採用しない。
- マイクロサービスは各コネクタを独立して拡張できるが、初期の運用対象を増やすため採用しない。

## 非機能要件

### 可用性

- 公開ベータの月間Web稼働率は99.5％をSLOとし、SLAとして保証しない。
- 巡回ワーカーのプロセス障害や過負荷がWeb画面、Hono API、読書記録を停止させない構成にする。
- asterion-manifestで定義されているノードは`plexus`一台のため、ノード、電源、家庭回線の障害ではサービス全体が停止する。
- 単一ノードではレプリカを増やしてもノード障害へ耐えられないため、Web、API、ワーカーは一レプリカから始める。
- ヘルスチェックでWeb、API、ワーカー、データベース接続を監視する。
- 家庭内ネットワークの外からWebとCloudflare Tunnelを監視する。
- Webの重大エラーは5分以内に管理者へ通知する。
- コネクタは3回連続で失敗した時点で管理者へ通知する。

### 性能

- モバイルの75パーセンタイルでLCPを2.5秒以下にする。
- モバイルの75パーセンタイルでINPを200ミリ秒以下にする。
- CLSを0.1以下にする。
- 認証済みの主要操作は95パーセンタイルで1.5秒以内に応答する。
- 公開作品ページはCDNキャッシュを利用する。

### 更新鮮度

- 利用条件が許す対応サイトでは、更新の95％以上を公開から30分以内に検出する。
- Web Pushの95％以上を更新検出から5分以内に送信する。
- コネクタのジョブ滞留が10分を超えた場合は警告する。

### データ保全

- RPOを15分、RTOを4時間とする。
- 既存PostgreSQLの32GiB local-path PVCは単一ノードにあるため、PVCだけをバックアップとして扱わない。
- WAL-Gで週次の物理ベースバックアップを作成し、PostgreSQLのWALを15分以内の間隔でCloudflare R2へ退避する。
- 日次の暗号化済み論理バックアップをCloudflare R2へ30日間保存する。
- WAL-Gの導入と別環境への復元成功を公開ベータの前提条件にする。
- Asterionを1時間以内に復旧できない場合は、一時VPSへ単一ノードk3sを作り、R2から復元してRTOを満たす。
- 月に一度、別データベースへの復元手順を確認する。
- 統合、分割、削除などの運営操作を監査ログへ残す。

### セキュリティ

- Cookieへ`HttpOnly`、`Secure`、適切な`SameSite`属性を設定する。
- CSRF、XSS、クリックジャッキング、SQLインジェクションを防ぐ。
- ユーザー入力はプレーンテキストとして保存し、表示時にエスケープする。
- 外部リンクへ`rel="nofollow ugc noopener"`を設定する。
- コネクタの接続先をホスト許可リストで制限し、SSRFを防ぐ。
- 登録、ログイン、感想、いいね、通報へレート制限を設定する。
- スパムが観測された段階でCloudflare Turnstileを追加する。
- 外部公開はCloudflare Tunnelだけを使い、アプリのServiceはClusterIPに限定する。
- Web、API、ワーカーは非root、読み取り専用ルートファイルシステム、不要なLinux capabilityの削除を設定する。
- CNIでNetworkPolicyを強制できることを確認し、Web、API、ワーカー、PostgreSQL間の必要な通信だけを許可する。
- アプリのSecretはSOPSとageで暗号化し、平文をGitへ保存しない。
- Argo CDとAdminerの既存ホスト名にCloudflare Accessが適用されていることを確認し、未適用なら公開ベータ前に保護または外部経路を削除する。
- 管理者にはパスキーまたは二要素認証を必須にする。
- 権限を一般利用者、モデレーター、管理者に分ける。
- アップロード画像のMIMEタイプ、容量、画像寸法を検証し、位置情報を除去する。

### プライバシー

- 実名、生年月日、住所を収集しない。
- 標準公開範囲と記録単位の上書きをすべての表示経路へ適用する。
- 非公開データを検索インデックス、OG画像、通知本文へ含めない。
- ブロック時は相互フォローを解除し、活動と通知を非表示にする。
- アカウント削除後、運用ログとバックアップを除く個人データを30日以内に削除する。
- プライバシーポリシー、利用規約、削除依頼、著作権侵害の連絡窓口を公開前に用意する。

### アクセシビリティ

- WCAG 2.2 AAを目標とする。
- キーボードだけで登録、検索、フォロー、既読操作、設定変更を行えるようにする。
- フォーカス表示、見出し構造、フォームラベル、エラー説明を提供する。
- 色だけで読書状態、未読、エラーを区別しない。
- ネタバレの開閉状態をスクリーンリーダーへ通知する。

### 運用性

- コネクタごとに取得成功率、取得時間、更新件数、解析失敗件数を記録する。
- Webのエラー率、応答時間、ジョブ滞留数、通知失敗率、メールバウンス率を監視する。
- AsterionのノードCPU、メモリ、ディスク、PVC使用率、Pod再起動回数、cloudflaredのReady状態を監視する。
- Argo CDの同期失敗と構成差分を通知する。
- ログへ認証トークン、メール本文、Push鍵を出力しない。
- データベースマイグレーションはWebのデプロイ前に一度だけ実行する。
- コネクタの追加は既存コネクタを変更せず、フィクスチャとともに行えるようにする。

## 将来の収益化

初期版は無料ベータとし、広告、アフィリエイト、決済を実装しない。

将来の収益化候補は次の順で検討する。

1. 開発を支援する任意のサポータープラン。
2. 紙と電子の単行本購入リンクによるアフィリエイト。
3. 非パーソナライズ広告。

課金方式が決まる前に請求、契約、権限テーブルを作らない。

取得元ごとに営利利用、広告利用、アフィリエイト利用の可否を記録し、収益化前に再確認する。

公式閲覧リンクと購入リンクを別のデータとして保持し、アフィリエイト導入時も公式URLを書き換えない。

有料機能を決めた段階で機能権限と決済を追加し、無料版の読書記録とデータエクスポートは維持する。

## インフラコスト

### 為替の基準

2026年7月24日17時06分42秒（日本標準時）に確認したUSD/JPYの市場参考値は、**1ドル163.7270円**である。

参照元は[Barchartのデータを表示するPound Sterling Live](https://www.poundsterlinglive.com/data/currencies/usd-pairs/USDJPY-exchange-rate)であり、表示値には15分の遅延がある。

為替は連続して変わるため、「常に正確な1ドルの値段」は存在しない。

カード会社や銀行から実際に請求される円額には、決済時の為替レート、為替手数料、税が使われる。

以下のドル建て料金は1ドル163.7270円で換算し、1円単位へ四捨五入する。

### 採用する最小構成

公開ベータまでは、すでに常時稼働しているAsterion k3sクラスタを使う。

既存の家庭回線、`cp20.dev`のサブドメイン、Cloudflare Tunnel、Argo CD、PostgreSQLを再利用できる場合、新たな固定インフラ費は月0円である。

GitHub ActionsとGHCR、Cloudflare R2、Resendは無料枠内から始める。

家庭回線とクラスタがこのサービスのために新設されたわけではないため、既存の基本料金は増分コストへ含めない。

ただし、追加の消費電力、故障時の交換部品、UPS、開発者の運用工数は0円ではない。

| 段階                     | 構成                                                                |  新たな月額の目安 |
| ------------------------ | ------------------------------------------------------------------- | ----------------: |
| ローカル開発             | 開発端末、GitHub Actions無料枠                                      |             月0円 |
| 限定アルファと公開ベータ | Asterion、既存PostgreSQL、Cloudflare Tunnel、R2無料枠、Resend無料枠 | 月0円と追加電気代 |
| R2無料枠超過             | Standardストレージ                                                  |     月約2.46円/GB |
| 有料メールを追加         | Resend Pro月20ドル                                                  |   月3,275円を加算 |
| クラスタ外への一時退避   | XServer VPS 4GB一台へ単一ノードk3sを作る                            |         月2,200円 |

追加電気代は、次の式で実測した平均消費電力から求める。

```text
追加電気代 = 追加平均消費電力(W) × 24時間 × 30日 ÷ 1,000 × 実際の電力量単価(円/kWh)
```

たとえば平均10W増え、電力量単価を35円/kWhと仮定すると、追加電気代は月252円になる。

Cloudflare R2は、Standardクラスで月10GB、100万回の書き込み、1,000万回の読み込みまで無料である。

無料枠を超えたStandardストレージは月0.015ドル/GB（現在の換算で約2.46円/GB）である。

Resendは月3,000通まで無料であり、Proは月20ドル（現在の換算で3,275円）からである。

Web Pushにはサービス側の配信通数に応じた直接費用がない。

即時通知はWeb Pushを基本とし、メールは更新があった日だけ送る任意のダイジェストにする。

ニコニコ漫画の全件収録で増えやすい費用は、バックフィル中のワーカーCPUとPostgreSQLの保存領域である。

通常巡回を優先し、バックフィルは残ったCPU時間だけで低速実行する。

ブラウザバイナリを本番へ入れずHTTP取得だけにすることで、追加の月額サービス費を発生させず、ワーカーのCPU、メモリ、コンテナ保存領域を抑える。

バックフィルのためだけに家庭内ノードやVPSを増やさず、余剰資源の範囲で処理する。

Asterionのリポジトリにはノードのハードウェア容量と現在の使用率が記録されていないため、第0段階で実測する。

次のいずれかが起きた場合に限り、サービス全体をクラスタ外へ移す。

- 既存ワークロードを含むノードのメモリ使用率の95パーセンタイルが一週間にわたって75％を超える。
- 通常巡回のジョブ滞留が10分を超える状態が続く。
- PostgreSQLのPVCまたはノードディスク使用率が70％を超える。
- 実測の月間稼働率が2か月続けて99.5％を下回る。
- 家庭回線の送信元IPが取得先から継続的に制限される。
- 障害復旧作業が月2時間を超える。

通常月の追加インフラ予算は月1,000円以内とし、一時退避用に月3,000円を支出できる状態を保つ。

## 開発ロードマップ

### 第0段階

期間は2週間とする。

- 検証済みのニコニコ漫画、少年ジャンプ＋、コミックDAYS、となりのヤングジャンプ、カドコミから、漫画本文と画像を除いた最小HTMLまたはAtomフィクスチャを作る。
- 利用規約、robots.txt、API、フィード、問い合わせ先を確認する。
- 本番収集を許可できる対象だけを`SourcePolicyRecord`へ登録する。
- Bun上でNext.js、Hono、Hono RPC、Better Auth、Drizzle ORM、Graphile Worker、`web-push`の互換性スパイクを行う。
- `Bun.fetch`、Cheerio、Valibotで5対象のコネクタ試作を実装し、ブラウザなしで差分取得できることを自動テストする。
- domain、application、contracts、adapter、composition rootのworkspace骨格を作り、逆向き依存をoxlintで拒否する。
- oxfmtとoxlintをCIへ組み込み、`bun run check`を必須checkにする。
- 作品、掲載先、話、分割掲載の重複モデルを実データで検証する。
- openBDと国立国会図書館サーチの収録率を確認する。
- 主要画面の操作プロトタイプを作る。
- AsterionのCPU、メモリ、ノードディスク、PostgreSQL PVCの使用量を一週間計測する。
- Cloudflare Accessが既存のArgo CDとAdminerへ適用されていることを確認する。
- WAL-GでAsterion外へバックアップし、別環境へ復元できることを確認する。
- GHCRの検証用イメージをArgo CDから配備し、Cloudflare Tunnel経由で到達できることを確認する。

### 第1段階

期間は3週間から4週間とする。

- identityのdomain、use case、Better Auth adapterを分けて認証、プロフィール、公開範囲を実装する。
- catalogのdomain、use case、DB adapterを分けて作品、作者、掲載先、話、巻のスキーマを実装する。
- query portとread modelを使って作品検索と作品ページを実装する。
- 管理画面と監査ログを実装する。
- Graphile Workerとコネクタ共通基盤を実装する。
- GitHub Actionsでコンテナを構築してGHCRへ公開する。
- asterion-manifestへWeb、API、ワーカー、Service、Secret、マイグレーションJobを追加する。
- 既存PostgreSQLへ専用データベースとロールを作る。
- cloudflaredへ公開ホスト名とServiceの対応を追加する。
- ニコニコ漫画の公式作品と2つの公式サイトへ対応する。

### 第2段階

期間は3週間から4週間とする。

- 読書状態、既読、追いつき状態を実装する。
- 利用者単位の掲載先優先順位と4つのフォロー方式を実装する。
- 単行本の既読、紙と電子の所蔵、新刊通知を実装する。
- 巻と話の対応付けを実装する。
- アプリ内通知、Web Push、メールダイジェストを実装する。
- PWAとしてホーム画面へ追加できるようにする。
- WXT拡張と確認画面を実装し、初期5サイトのお気に入りをインポートできるようにする。

### 第3段階

期間は3週間から4週間とする。

- 利用者フォローと承認を実装する。
- タイムライン、感想、いいねを実装する。
- ネタバレ制御を実装する。
- ブロック、ミュート、通報、モデレーションを実装する。
- Web Share API、SNS共有リンク、OG画像を実装する。

### 第4段階

期間は4週間から6週間とする。

- 許可された10サイトへコネクタを拡張する。
- ニコニコ漫画の活動中ユーザー投稿作品を取り込む。
- 残りのユーザー投稿作品の低速バックフィルを開始する。
- 負荷試験、アクセシビリティ試験、セキュリティ確認を行う。
- バックアップからの復元訓練を行う。
- 招待制ベータを経て一般公開する。

経験者2人では16週間から20週間、1人では24週間から30週間を目安とする。

ニコニコ漫画の全過去作品の収録完了は一般公開後も継続する。

## テスト計画

### 単体テスト

- 読書状態と追いつき状態を正しく分離できる。
- 指定した話までの一括既読と取り消しが正しく動く。
- 利用者単位の掲載先優先順位から対象掲載先を選べる。
- 4つのフォロー方式が期待する通知対象を返す。
- 同じイベントから同じ通知を二重生成しない。
- 巻の既読から確認済みの収録話だけを既読にできる。
- 紙と電子の所蔵状態を独立して保存できる。
- 公開範囲の標準値と記録単位の上書きを正しく解決できる。
- 未読話の感想をネタバレとして判定できる。
- 作品名、話数、枝番、URLを正規化できる。

### コネクタテスト

- 保存した最小HTML、Atom、埋め込みJSONのフィクスチャから作品と話を抽出できる。
- 共通フィード型コネクタを少年ジャンプ＋、コミックDAYS、となりのヤングジャンプの各フィクスチャへ適用できる。
- ニコニコ漫画の更新一覧で透かし位置まで走査し、変更された作品だけを取得対象にできる。
- ニコニコ漫画の公式作品、ユーザー投稿作品、種別不明を安全に区別できる。
- カドコミの埋め込みJSONをValibotで検証し、想定外の構造を解析失敗として扱える。
- 同じHTMLを再処理してもデータが重複しない。
- HTML要素が欠けた場合に既存データを削除しない。
- ETagとLast-Modifiedを次回リクエストへ渡し、304では解析しない。
- タイムアウト、本文上限超過、誤ったContent-Type、許可外ホストへのリダイレクトを拒否できる。
- テスト用HTTPサーバーで、漫画ページ画像へのリクエストが一件も発生しないことを確認する。
- 文字コード、全角数字、丸数字、枝番を処理できる。
- 削除、公開終了、掲載URL変更を区別できる。
- R18または年齢確認付きデータを除外できる。

### 統合テスト

- 巡回結果、ReleaseEvent、通知を一つのトランザクション境界で整合させられる。
- 出版社サイトとニコニコ漫画の同じ話を一作品へ関連付けられる。
- 後から重複が判明しても既読と通知履歴を保持できる。
- 一括掲載と分割掲載を多対多で対応付けられる。
- バックフィルでは通知を出さず、その後の更新だけを通知できる。
- 期限切れPush購読を安全に削除できる。
- ブロック対象の活動と通知を返さない。

### E2Eテスト

- 登録、公開範囲選択、検索、フォロー、既読までを完了できる。
- 掲載先の優先順位を変更し、別作品にも同じ順位が反映される。
- 各フォロー方式で期待した更新通知を受け取れる。
- 単行本だけを使って既読、所蔵、新刊通知を管理できる。
- 感想を投稿し、未読の別利用者には伏せて表示される。
- 公開記録をSNS共有し、正しいOG情報が表示される。
- 非公開記録が公開URL、検索、タイムラインから閲覧できない。
- ブロック、通報、管理者による非表示が動作する。

### 運用試験

- コネクタの連続失敗を検出し、自動停止と通知を行える。
- ワーカー停止中に蓄積したジョブを再開後に重複なく処理できる。
- PostgreSQLを指定時点へ復元し、APIとワーカーを再接続できる。
- バックフィル中も通常更新を優先して処理できる。
- 公開ベータ相当のアクセスと通知数で性能目標を満たせる。
- Web、API、ワーカー、cloudflaredのPodを個別に停止し、Argo CDまたはKubernetesによって復旧できる。
- ワーカーを上限まで動かしても既存のAsterionワークロードが停止しない。
- Asterion停止中もR2のバックアップから別環境へ復元できる。

## 公開判定

次の条件を満たした時点で一般公開する。

- ニコニコ漫画を含む、利用条件を確認済みの10サイトへ対応している。
- ニコニコ漫画の公式作品と活動中のユーザー投稿作品を収録している。
- 作品、掲載先、話、分割掲載の統合を管理画面から修正できる。
- Web話と単行本の両方で読書管理できる。
- アプリ内通知、Web Push、メールダイジェストが動作する。
- 公開範囲、ネタバレ、ブロック、通報がすべての表示経路へ適用される。
- バックアップからの復元手順を一度完了している。
- Asterionに2GiB以上のメモリと1コア相当のCPU余力がある。
- PostgreSQL PVCとノードディスクの使用率が70％未満である。
- 家庭外からの稼働監視と障害通知が動作している。
- Argo CDとAdminerの外部経路がCloudflare Accessで保護されているか、削除されている。
- 利用規約、プライバシーポリシー、削除依頼、著作権侵害の窓口を公開している。
- 重大なアクセシビリティ違反と既知の高危険度脆弱性が残っていない。

## 初期対象外

- 漫画本文を読むための独自ビューア。
- iOSとAndroidのネイティブアプリ。
- 第三者向け公開API。
- AIによる推薦。
- AIによる作品と話の自動統合。
- ダイレクトメッセージ。
- スレッド形式の返信。
- 感想への画像添付。
- 単行本の購入価格、購入店、貸出管理。
- R18作品。
- 広告、アフィリエイト、決済。
- 読書管理サービスからの一括インポート。
